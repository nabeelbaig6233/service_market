/**
 * Organize All The Actions, Getters etc.
 * ==========================================
 * Register the actions, getters, mutations,
 * state here, so we'll have to import only
 * one file
 * ==========================================
 */

export { actions } from './store/actions/actions';
export { getters } from './store/getters/getters';
export { mutations } from './store/mutations';
export { states } from './store/states/states';
